node_modules
# Virtual Environment (The "node_modules" of Python)
blog-env/

# Python Cache (Compiled files that speed up execution)
__pycache__/
*.pyc

# Local Database (Your data, not your code)
blog.db

# VS Code Settings (Optional, but keeps things clean)
.vscode/